version: 2.1
orbs:
  ci: bigcommerce/internal@volatile

jobs:
  pre-setup-test:
    docker:
      - image: circleci/node:10-browsers
    resource_class: small
    steps:
      - ci/pre-setup

workflows:
  test-pre-setup:
    jobs:
      - pre-setup-test